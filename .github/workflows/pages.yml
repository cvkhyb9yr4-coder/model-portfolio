name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Prepare site artifact
        shell: bash
        run: |
          mkdir -p _site
          shopt -s nullglob
          root_files=( *.html *.css *.js )
          if [ "${#root_files[@]}" -gt 0 ]; then
            cp "${root_files[@]}" _site/
          fi
          for file in CNAME robots.txt sitemap.xml favicon.ico favicon.png favicon.svg manifest.webmanifest; do
            if [ -e "$file" ]; then
              cp "$file" _site/
            fi
          done
          find . -maxdepth 1 -mindepth 1 -type d \
            ! -name '.git' \
            ! -name '.github' \
            ! -name '_site' \
            -exec cp -R {} _site/ \;

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
